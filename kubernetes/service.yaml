apiVersion: v1
kind: Service
metadata:
  name: secure-shop
  namespace: secure-shop
spec:
  selector:
    app: secure-shop
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8070
      nodePort: 30070
  type: NodePort
